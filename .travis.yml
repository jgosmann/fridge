language: python
python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
install:
    - pip install -r requirements.txt
    - pip install coveralls
    - pip install pytest-cov
script:
    - pip install .
    - py.test --cov fridge fridge
    - py.test systemtests
after_success:
    coveralls
